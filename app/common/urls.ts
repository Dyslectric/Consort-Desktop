// Where this project lives. Anywhere the app links to itself -- release notes,
// the issue tracker, the licence, the About dialog -- should come from here, so
// that a move costs one edit rather than a search.

export const repoUrl = "https://github.com/Dyslectric/Consort-Desktop";
export const releasesUrl = `${repoUrl}/releases`;
export const issuesUrl = `${repoUrl}/issues`;

// The server this is a client for. Linked from "create a new organization",
// since a Consort organization is something you deploy rather than sign up for.
export const serverRepoUrl = "https://github.com/Dyslectric/consort";
